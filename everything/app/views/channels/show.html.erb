<% provide(:title, "All channels") %>

<h1><%= @channel.name %>'s videos</h1>

<ul>

  <% @videos.each do |v| %>

    <li><%= link_to v.title, video_path(v) %></li>

  <% end %>

  <br>

</ul>

<%= will_paginate @videos %>
