<% provide(:title, "Recently uploaded videos") %>

<h1>Recently uploaded videos</h1>

<%= render 'shared/list_of_links', objects: @videos, options: { info: [:name, :time, :channel_name], thumbnails: true } %>
